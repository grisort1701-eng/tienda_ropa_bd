SELECT e.id_empleado, e.nombre
FROM empleado e
WHERE NOT EXISTS (
  SELECT p.id_pedido
  FROM pedido p
  WHERE NOT EXISTS (
    SELECT 1
    FROM pedido p2
    WHERE p2.id_pedido = p.id_pedido
      AND p2.id_empleado = e.id_empleado
  )
);
